# Lambda deployment dependencies (minimal)
# This file is used in Dockerfile for Lambda

# Web framework
fastapi==0.115.12
python-multipart==0.0.20
mangum>=0.17.0  # Lambda adapter (REQUIRED for Lambda)

# Database
asyncpg>=0.29.0  # PostgreSQL adapter

# AWS
boto3>=1.28.0

# Computer Vision
opencv-python-headless==4.11.0.86  # Headless for Lambda (no GUI)
ultralytics==8.3.145  # YOLO v8
mediapipe==0.10.21  # Pose estimation
face_recognition>=1.3.0

# AI/LLM
anthropic==0.52.0  # Claude API

# Utils
numpy<2.1
Pillow==11.2.1
python-dotenv==1.1.0
pydantic==2.11.3
httpx==0.28.1
python-jose[cryptography]
bcrypt

# NOTE: uvicorn NOT included - Lambda doesn't need it!
