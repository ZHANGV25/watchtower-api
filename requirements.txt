# Web framework
fastapi==0.115.12
uvicorn[standard]==0.34.2  # For local development only (not needed in Lambda)
python-multipart==0.0.20
mangum>=0.17.0  # Lambda adapter (not needed locally)

# Database
asyncpg>=0.29.0  # PostgreSQL adapter

# AWS
boto3>=1.28.0

# Computer Vision
opencv-python-headless>=4.8.0  # Headless for Lambda (no GUI dependencies)
ultralytics>=8.3.0  # YOLO v8 (flexible version for Python 3.13)
mediapipe>=0.10.30  # Pose estimation (Python 3.13 compatible)
# face_recognition>=1.3.0  # Face recognition - commented out (not Python 3.13 compatible yet)

# AI/LLM
anthropic==0.52.0  # Claude API

# Utils
numpy<2.1
Pillow==11.2.1
python-dotenv==1.1.0
pydantic==2.11.3
httpx==0.28.1
python-jose[cryptography]  # JWT tokens
bcrypt  # Password hashing

# Removed:
# - aiosqlite (replaced with asyncpg for PostgreSQL)
# - websockets (no WebSocket support in simplified version)
